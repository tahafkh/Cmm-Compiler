struct A begin
    int kir
    bool kos
    fptr<void->int> koon
end

struct A ff(int x, int y) begin
    struct A dool
    dool.kir = 12
    dool.kos = true

    return dool
end

main() begin
    list #fptr<int, int-> struct A> li

    append(li, ff)
    fptr<int, int-> struct A> x = li[0]
    int len = x(2,3).kir
    display(len)
end








